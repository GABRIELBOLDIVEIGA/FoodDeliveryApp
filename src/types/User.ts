export type User = {
  userId: string;
  name: string;
  email: string;
  zipCode: string;
  neighborhood: string;
  street: string;
  city: string;
  number: string;
  phoneNumber: string;
  role: string;
};
